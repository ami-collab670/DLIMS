import { JOB_STATUS_OPTIONS } from "@/lib/job-order-labels";

export const SAMPLES_PAGE_SIZE = 20;

/** Same pipeline as job orders: submitted → received → … → completed */
export const SAMPLE_STATUS_OPTIONS = JOB_STATUS_OPTIONS;
